<h1>My Cases</h1>

  <% if current_user.is_broker? %>
    <%= form_tag '', :method => :get do %>
      <%= select_tag :active, options_for_select(["Archive", "Offered & Awaiting completion", "Active"]), :prompt => 'Select One' %>
      <%= submit_tag 'Filter' %>
    <% end %>
  <% end -%>

<%= render 'case_list' %>
