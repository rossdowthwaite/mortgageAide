<%= form_for(@client_agent) do |f| %>
  <% if @client_agent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client_agent.errors.count, "error") %> prohibited this client_agent from being saved:</h2>

      <ul>
      <% @client_agent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :client_id, :value => @user.id %>

  <div class="field">
    <%= f.label :agent_id %><br>
    <%= f.select :agent_id, User.agents.collect { |p| [ p.contact.full_name, p.id ] }, :prompt => 'Select One' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
